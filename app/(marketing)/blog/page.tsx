import { BlogPageView } from "@/components/features/marketing/BlogPageView";

export const metadata = {
  title: "Stories of Connection | FoodBridge",
  description:
    "Real stories from donors, volunteers, and NGOs — how surplus food becomes community impact across India.",
};

export default function BlogPage() {
  return <BlogPageView />;
}